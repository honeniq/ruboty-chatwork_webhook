RSpec.describe Ruboty::ChatworkWebhook do
  it "has a version number" do
    expect(Ruboty::ChatworkWebhook::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
